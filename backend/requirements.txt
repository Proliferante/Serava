# --- consola interna (app/api/admin.py + app/services/admin/) -------------
requests
beautifulsoup4
fastapi
uvicorn
pandas
numpy
scipy
scikit-learn
shapely
psycopg2-binary
sqlalchemy
python-dotenv

# Subida de las fotos de la ficha del predio (app/api/flujo.py). FastAPI lo
# exige para leer `multipart/form-data`: sin él, el endpoint ni siquiera se
# registra y el arranque falla con un RuntimeError.
python-multipart

# --- sesión y usuarios internos (app/api/auth.py, app/core/security.py) ---
# passlib con bcrypt para las contraseñas. bcrypt se fija por debajo de 4.1
# porque a partir de ahí passlib 1.7.4 no puede leer su número de versión y
# lanza un aviso en cada hash.
#
# `python-jose[cryptography]` estuvo aquí para firmar tokens JWT y se quitó
# con ellos: la sesión tiene estado en la tabla `sesiones`, no va firmada.
passlib[bcrypt]
bcrypt<4.1

# --- backend oficial (inmuebles, dashboard, notificaciones) ---------------
# TODO (backend oficial): agregar aquí lo que haga falta (Alembic, pytest,
# ruff, etc. — ver recomendacion_backend.pdf). fastapi/uvicorn/sqlalchemy/
# psycopg2-binary ya están arriba, no hace falta repetirlos.
